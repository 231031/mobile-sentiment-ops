services:
  backend:
    image: mlops_backend:latest
    build:
      context: .
      dockerfile: app/Dockerfile.backend

  frontend:
    image: mlops_frontend:latest
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=${VITE_API_URL}